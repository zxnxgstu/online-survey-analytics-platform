# Security

Do not commit `.env` files, database passwords, JWT secrets, or production credentials.
If a secret is ever committed, rotate it immediately and remove it from Git history before publishing the repository.
